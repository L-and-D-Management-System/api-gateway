name: CI

on:
  push:
    branches: [ main, develop ]

jobs:
  call-workflow:
    uses: L-and-D-Management-System/LnD/.github/workflows/reusable-ci.yaml@main
    with:
      service_name: api-gateway
      image_name: api-gateway
    secrets: inherit